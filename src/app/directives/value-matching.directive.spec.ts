import { ValueMatchingDirective } from './value-matching.directive';

describe('ValueMatchingDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueMatchingDirective();
    expect(directive).toBeTruthy();
  });
});
